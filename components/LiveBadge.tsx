"use client";import {useEffect,useState} from 'react';export function LiveBadge(){const [live,setLive]=useState(false);useEffect(()=>{const poll=()=>fetch('/api/live').then(r=>r.json()).then(d=>setLive(!!d.live)).catch(()=>{});poll();const id=setInterval(poll,60000);return()=>clearInterval(id)},[]);return(<span className={`inline-flex items-center gap-1.5 px-2 py-1 rounded-full ${live?'bg-red-500/20 text-red-300':'bg-white/10 text-white/70'}`}><span className={`w-2 h-2 rounded-full ${live?'bg-red-400 animate-pulse':'bg-white/40'}`}/>{live?'LIVE':'Offline'}</span>)}