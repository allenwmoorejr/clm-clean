"use client";import {useEffect,useState} from 'react';export default function Events(){const[e,sE]=useState([] as any[]);useEffect(()=>{fetch('/events.json').then(r=>r.json()).then(sE).catch(()=>{})},[]);return(<section className='container py-16'><h1>Events</h1><p className='text-white/70 mt-2'>Upcoming gatherings and special services.</p><div className='mt-8 grid gap-6'>{e.map(ev=>(<div key={ev.id} className='card p-6 flex flex-col md:flex-row md:items-center md:justify-between gap-4'><div><h3>{ev.title}</h3><p className='text-white/70 mt-1'>{new Date(ev.date).toLocaleString()} {ev.end?`– ${new Date(ev.end).toLocaleTimeString()}:''}`}</p>{ev.location&&<p className='text-white/60'>{ev.location}</p>}{ev.description&&<p className='text-white/70 mt-2'>{ev.description}</p>}</div><a className='btn-primary' href={`/api/ics?title=${encodeURIComponent(ev.title)}&desc=${encodeURIComponent(ev.description||'')}&start=${encodeURIComponent(ev.date)}&end=${encodeURIComponent(ev.end||ev.date)}&loc=${encodeURIComponent(ev.location||'Christ Like Ministries, Birmingham, AL')}`}>Add to Calendar</a></div>))}</div></section>)}